# 变更记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

## [1.1] - 2026-09-15

### 新增
- 批量移动：文件夹空白处右键「批量移动…」，在系统文件对话框内多选，一次处理任意数量项目
- 失败列表对话框：逐项列出失败对象与原因，支持「复制失败信息」
- `build.cmd` 与 Release 附件输出 SHA256 校验值
- `uninstall.cmd` 卸载脚本

### 变更
- **多选文件时右键不再显示「移动到…」**：Windows 对经典右键动词按「每文件一个进程」
  派发，一次调用拿不到全部路径，v1.0 因此只移动了光标所指的那一个；现改为多选时
  隐藏该项，批量统一走「批量移动…」入口
- 退出码语义明确：批量中只要有项目失败即返回 2
- 位置记忆仅当至少一项移动成功后才更新

### 工程
- `tests/` 入库，`build.cmd` 自动运行 45 项断言，失败即中断构建
- 新增 `error_report` 模块（内存 `DLGTEMPLATE`，无资源脚本依赖）

## [1.0] - 2026-09-13

### 新增
- 单文件/文件夹「移动到…」右键菜单项（同一卷内快速移动）
- 系统原生文件夹选择框，自动记忆上次目标位置
- 同卷校验、同名不覆盖、系统关键目录黑名单
- 失败原因映射为中文提示
- `install.cmd` 安装脚本（仅写 HKCU，无需管理员权限）
