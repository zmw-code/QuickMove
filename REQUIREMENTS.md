# QuickMove（快捷式文件转移）需求分析

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.0（对应代码 v1.0 基线，规划至 v2.x） |
| 日期 | 2026-09-14 |
| 来源 | `file_easy_transfer.txt` 缺口清单（8 条，归并为 6 个特性）；`src/` 现有实现（FR-01~FR-04）；2026-09 阶段路线初稿（经评审，修订点见 §9 决策记录） |
| 状态 | FR-05 / FR-06 已细化到可直接开发；FR-07~FR-10 为方案级，随阶段推进补充 |

## 1. 产品定位

资源管理器右键“移动到…”：把「复制 → 切换目录 → 粘贴 → 删除源」压缩成两次点击。

三条产品原则（所有 FR 的上位约束）：

1. **能复用系统 UI 的绝不自制 GUI**——原生 `IFileOpenDialog`、原生消息框，仅在有硬需求（进度、错误列表）时用最小原生对话框补位。
2. **不做“更快的复制”，做“更安全的移动”**——显式确认、绝不静默降级、删除源前必校验、失败必须可见且可回滚。
3. **极致轻量**——C++17 + 纯 Win32 API、零第三方依赖、单文件 exe、独立进程崩溃隔离、无后台常驻。

## 2. 现状基线（v1.0 已完成）

| FR | 内容 | 实现锚点 |
| --- | --- | --- |
| FR-01 | 右键菜单集成：HKCU 注册 `*` 与 `Directory` 两项，`MultiSelectModel=Single`，免管理员安装 | `install.cmd` / `uninstall.reg` |
| FR-02 | 参数解析与源校验：引号剥离、`GetFullPathNameW`+`GetLongPathNameW` 规范化（防 `..`/8.3 绕过）、存在性、260 长度、KNOWNFOLDERID 黑名单 + 盘符根/UNC 根拦截 | `path_utils.cpp` |
| FR-03 | 目标选择与位置记忆：`IFileOpenDialog`（`FOS_PICKFOLDERS`），`last_used_path` 原子写（tmp→replace），失效回退“文档” | `folder_picker.cpp` / `config_store.cpp` |
| FR-04 | 同卷移动执行：`MoveFileExW(src,dst,0)` 不带 `COPY_ALLOWED` 保证跨卷必失败不降级；错误码→中文映射；目标四项校验（非目录/原地/源内/超长） | `file_move.cpp` / `main.cpp` |

v1 明确不做（且已在 README 向用户声明）：批量、跨卷、覆盖/重命名选项、设置面板、收藏、>260 长路径。

## 3. 缺口清单与功能映射

`file_easy_transfer.txt` 的 8 条缺口去重归并如下（其中 #1/#5 同属跨卷，#4/#7 同属 Win11 菜单）：

| txt # | 缺口摘要 | 归并特性 | FR | 阶段 |
| --- | --- | --- | --- | --- |
| 2 | 批量移动，MultiSelectModel，进度与错误汇总 | 批量移动（多对一） | FR-05 | 1（v1.1） |
| 1、5 | 轻度跨卷支持，显式确认；复制+校验+删除源一键化，处理权限/时间戳/占位/大文件，带进度与失败回滚 | 跨卷安全移动 | FR-06 | 1（v1.2/v1.3） |
| 6 | 移动撤销与历史：追加式日志 + 一键回退到原位 | 撤销与历史 | FR-07 | 2 |
| 3 | 收藏/固定常用目录，对话框内直接可选 | 常用目录收藏 | FR-08 | 2 |
| 4、7 | Win11 一级右键菜单（MSIX + IExplorerCommand），配签名与 Winget/Chocolatey 分发 | Win11 一级菜单 | FR-09 | 3 |
| 8 | 受管/合规：黑名单反转为白名单 + 审计日志 | 受管/合规模式 | FR-10 | 4 |

## 4. 阶段路线

| 阶段 | 版本 | 目标 | 交付 |
| --- | --- | --- | --- |
| 0 稳定化与冷启动 | v1.0.x | 建立信任，解决生存问题 | 见 §4.1 P0 清单 |
| 1 内核进化 | v1.1 → v1.2 → v1.3 | 从“能用”到“好用” | FR-05 → FR-06a → FR-06b |
| 2 体验超越 | v1.4/v1.5 | 从“好用”到“离不开” | FR-07、FR-08 |
| 3 生态破局 | v2.0 | Win11 一级菜单 + 分发 | FR-09 |
| 4 垂直突围 | v2.x | 企业合规 | FR-10 |

**阶段 1 拆分修订**：原路线将“批量 + 跨卷”并为一个 v1.x。现拆为三个可独立发布的小版本：

- **v1.1 批量移动**（FR-05）——改动面小、风险低、先出货验证多选链路；
- **v1.2 单文件跨卷**（FR-06a）——打通“复制→校验→删除→回滚→日志”全链路；
- **v1.3 目录树跨卷 + 可选哈希**（FR-06b）——本产品工作量与风险最大的单点（递归、树删除、占位文件），单独成版。

### 4.1 阶段 0 P0 清单（先于一切新功能）

1. **杀软误报**：向 360 误报申诉平台提交样本；Release 页固定提供 SHA256（`build.cmd` 编译后自动输出并写入 `build\checksum.txt`）；README 增加校验方法说明。
2. **Win11 使用引导**：README 已有提示，补充到安装脚本完成提示中（“Win11 请点显示更多选项或 Shift+F10”）。
3. **边界测试集入库**：将 `build/selftest.cpp` 移入 `tests/` 并纳入版本控制，覆盖：长路径（恰 259/260）、特殊字符（空格/全角/emoji/保留名 `con`）、只读/隐藏/系统属性、权限不足、文件被占用、UNC 路径、目录含超长子树。

## 5. 功能需求

### FR-05 批量移动（v1.1）

**用户故事**：多选 N 个文件/文件夹 → 右键“移动到…” → 选一次目标 → 全部移入；失败项集中汇报，成功项不受牵连。

| # | 需求 | 说明 |
| --- | --- | --- |
| FR-05.1 | 菜单多选模型 | `MultiSelectModel` 由 `Single` 改为 `Player`（`install.cmd` 覆盖写值，重跑即升级） |
| FR-05.2 | 多源参数解析 | `argv[1..argc-1]` 均视为源路径（Explorer 多选时逐项传参），去重、逐项走 FR-02 校验链；预校验失败项直接进汇总，不中断其余项 |
| FR-05.3 | 单目标选择 | 仍为一次 `IFileOpenDialog`（多对一），初始目录逻辑不变 |
| FR-05.4 | 逐项目标校验 | 复用 `checkDestination`：同名冲突跳过该并计入失败；任一文件夹源的子树不可作为目标；目标无效整体终止（对话框阶段即可拦截） |
| FR-05.5 | 同卷约束 | v1.1 批量仅支持所有项与目标同卷；检测到跨卷项 → 该项拒绝，原因注明“跨卷移动将于 v1.2 支持”（见决策 D4） |
| FR-05.6 | 执行与汇总 | 循环 `moveByRename`；结束弹轻量结果：全部成功=完成提示；存在失败=失败列表对话框（纯 Win32 对话框 + 多行只读文本 + “复制失败信息”按钮写 `CF_UNICODETEXT` 剪贴板），不引入重型 GUI |
| FR-05.7 | 记忆与退出码 | ≥1 项成功即更新 `last_used_path`；退出码 0=全部成功，1=未执行（取消/无有效项），2=存在执行失败（含部分成功） |

**非目标**：进度条（同卷 rename 毫秒级，预留接口即可）、覆盖/重命名策略（维持“同名跳过”）、跨卷混合批次。

**验收标准**：多选 2 文件一次移动成功；混合黑名单项时其余正常移动；全部失败时列表完整且“复制失败信息”入剪贴板；对话框取消时零改动；文件夹多选可用；重跑 `install.cmd` 后多选菜单可用。

### FR-06 跨卷安全移动（v1.2 单文件 / v1.3 目录树）

**原则**：显式确认、绝不静默降级、删除源前必校验、失败不留残局、全程日志。数据不丢失 > 不留残局 > 自动静默（见 D7）。

| # | 需求 | 说明 |
| --- | --- | --- |
| FR-06.1 | 触发与显式确认 | 同卷项照走 rename；跨卷项弹确认框：「将在两个磁盘间移动：复制 → 校验 → 删除源」，展示源/目标卷、总大小；确认后才执行 |
| FR-06.2 | 复制前预检 | 目标卷剩余空间 ≥ 源大小总和（`GetDiskFreeSpaceExW`）；同名冲突；**reparse point / 云占位文件（`FILE_ATTRIBUTE_REPARSE_POINT`、`RECALL_ON_*`）默认拒绝**并说明原因（复制会触发 OneDrive 全量下载/把链接实体化）；源可读性 |
| FR-06.3 | 复制 | `CopyFileExW`（进度回调、可取消）；复制后用 `SetFileTime` 将源的创建/访问/修改三个时间戳显式对齐到目标 |
| FR-06.4 | 校验（删除源的前提） | 默认：大小 + 最后修改时间比对；配置项 `verify_hash=true` 时叠加 SHA-256（BCrypt，纯系统 API，零依赖）。校验不过 → 删除目标副本 → 计失败 |
| FR-06.5 | 删除源 | 校验通过后 `DeleteFileW`；删除失败 → 尝试回滚（删除目标副本），恢复操作前状态并报错 |
| FR-06.6 | 回滚与残局 | 失败/取消时删除所有已复制目标项；回滚自身也失败（如目标副本被占用）→ 写入“未完成事务”日志并明确告知两处路径，不静默吞掉 |
| FR-06.7 | 进度 UI | 轻量原生对话框：当前文件名 + 字节进度条 + 取消按钮（取消即触发回滚）；这是硬需求，不属于“自制重型 GUI” |
| FR-06.8 | 日志埋点 | 追加式 JSONL：`%LOCALAPPDATA%\QuickMove\history.log`，事件 `begin/copied/verified/removed/rolled_back/done/failed/incomplete`，含 `tx`（事务号）、`src`、`dst`、`bytes`、`err`。**格式即 FR-07 数据源，本版定稿不再迁移** |
| FR-06.9 | 目录树（v1.3） | `FindFirstFileExW` 递归复制 → 逐文件校验 → 自底向上删源树；目录时间戳对齐；不跟随 junction/symlink 到树外；进度=字节+文件数双计数 |
| FR-06.10 | UNC/网络卷 | 支持；默认仅 size+mtime 校验（哈希对网络卷代价高，保持可选），提示网络速度风险 |

**非目标**：断点续传、限速、覆盖/重命名选项（维持“同名跳过”）、EFS 加密文件跨卷（明确拒绝并提示）、断电后的自动清理（依赖 `incomplete` 日志在下次启动时提示，属 FR-07 范畴）。

**验收标准**：C:→D: 单文件移动后内容/大小/修改时间一致且源已消失；中途取消后源目标两侧状态与操作前一致；空间不足在复制开始前被拦截；占位文件被拒且有明确原因；`history.log` 事件序列完整可回放；拔掉目标盘等极端失败不产生“两边都有”或“两边都没有”的静默结果。

### FR-07 移动撤销与历史（阶段 2）

- 数据源：FR-06.8 日志（同卷 rename 也写入 `begin/done` 事件，保证可撤销面完整）。
- 入口：托盘图标菜单或独立小窗（纯 Win32 列表），列最近 N 次（默认 20）成功移动。
- 撤销执行：目标位置仍存在同名项且校验未被动过（大小+mtime+tx 匹配）→ 反向移动（同卷 rename 零成本；跨卷撤销走 FR-06 全流程）；目标已被修改/占用 → 拒绝该项并说明。
- 启动时扫描 `incomplete` 事务 → 提示一键清理残局。
- 日志轮转：超阈值（如 5 MB）滚动为 `history.1.log`。

### FR-08 常用目录收藏（阶段 2）

- **主方案（修订，见 D2）**：配置 `favorites[]` → `pickFolder` 时对每项 `IFileOpenDialog::AddPlace(IShellItem, FDAP_TOP)`，注入**原生对话框左侧导航栏**。完全符合“复用系统 UI”哲学，零自制控件。注：原路线提到“Places 机制”，经典对话框的 Places 自定义 API 已随 Vista 移除，`IFileDialog::AddPlace` 是官方替代，需在 Win11 实测确认显示位置（见风险 R2）。
- 管理方式：v1.4 先支持配置文件手工维护 + “将本次目标加入收藏”勾选项；设置入口后续补。
- 备选（若 AddPlace 实测不理想）：对收藏目录调用 `pintohome` 动词固定到资源管理器“快速访问”（系统级生效，但影响面超出本工具，需用户显式同意）。

### FR-09 Win11 一级右键菜单（阶段 3）

- 重构为 MSIX 打包 + `IExplorerCommand`，菜单项直达 Win11 右键顶部区。
- 配套：代码签名（评估 SignPath 开源免费计划 vs 自购 OV 证书，见风险 R1）、Winget/Chocolatey 清单、SmartScreen 信誉积累。
- 迁移：安装器检测旧 HKCU 菜单项并清理，避免双入口。

### FR-10 受管/合规模式（阶段 4）

- 黑名单逻辑反转：策略文件/注册表策略键（`HKCU\Software\Policies\QuickMove`）下发**目标白名单**，仅允许移动到受控目录。
- 审计：所有移动操作记录本地审计日志，可选推送 Syslog/HTTP 至企业侧。
- 商业边界：开源版保持全功能免费，企业版提供管理台与技术支持；具体待定。

## 6. 非功能需求

- **形态**：单 exe、零第三方运行时、独立进程（崩溃不连带 Explorer）；C++17 + MSVC /W4。
- **安全**：删除源之前必有校验；任何降级（rename→copy）必须经用户显式确认；失败永远可见。
- **性能**：同卷批量 N 项 < 1s；一切写操作先过预检（空间/冲突/属性）。
- **隐私**：所有数据与日志仅存本机；日志含明文本地路径（本地工具可接受）。
- **兼容**：Windows 10 1903+ / Windows 11；中文与 Unicode 路径为一等公民。
- **可演进**：退出码三档语义保持稳定；`config.json` 向后兼容（读取时保留未知字段）；注册表键名不迁移。

## 7. 里程碑总表

| 版本 | FR | 一句话 | 核心验收 |
| --- | --- | --- | --- |
| v1.0.x | —（P0） | 信任建设 | 申诉提交、SHA256、tests/ 入库 |
| v1.1 | FR-05 | 多选一次移入 | 部分失败不牵连、错误列表可复制 |
| v1.2 | FR-06a | 单文件跨卷“复制→校验→删除” | 取消/失败零残局、日志可回放 |
| v1.3 | FR-06b | 目录树跨卷 + 可选 SHA-256 | 树一致性、占位文件安全拒绝 |
| v1.4/v1.5 | FR-07/08 | 撤销历史 + 收藏 | 一键回滚到原位、收藏出现在原生对话框 |
| v2.0 | FR-09 | Win11 一级菜单 + 签名 | 顶部直达、SmartScreen 通过 |
| v2.x | FR-10 | 白名单 + 审计 | 策略下发生效、审计可导出 |

## 8. 风险与开放问题

| # | 风险/问题 | 缓解 |
| --- | --- | --- |
| R1 | 杀软误报与 SmartScreen（无签名 exe 的长期成本） | 阶段 0 申诉+哈希；阶段 3 签名/MSIX 根治；评估 SignPath 开源免费计划 |
| R2 | `IFileOpenDialog::AddPlace` 在 Win11 的展示位置与折叠行为需实测 | FR-08 预留 `pintohome` 备选 |
| R3 | 云占位文件跨卷复制触发 OneDrive 全量下载 | FR-06.2 默认拒绝并给出可理解的原因 |
| R4 | Explorer 多选传参格式（引号/顺序/超长列表截断）以实机为准 | selftest 增加多参数用例，装真机验证 |
| R5 | 目录树删除中途失败可能留下半删树 | 自底向上删除 + `incomplete` 事务日志 + 下次启动提示清理 |
| R6 | 已装用户升级到 v1.1 需更新注册表 `MultiSelectModel` | `install.cmd` 覆盖写值即可，README 注明重跑安装 |
| R7 | 长路径（>260 / `\\?\`）暂不支持 | 维持 backlog；v1.x 若开启长效路径支持需重估全部校验函数 |

## 9. 决策记录

- **D1** 定位：不做“更快的复制”，做“更安全的移动”（复制+校验+回滚+可撤销）。
- **D2** FR-08 主方案由含糊的“Places 机制”落定为 `IFileOpenDialog::AddPlace`（官方 API、零自制 UI）。
- **D3** 阶段 1 拆为 v1.1/v1.2/v1.3 三个可发布小版本，降低“批量+跨卷+目录树”一次性交付风险。
- **D4** v1.1 批量仍限同卷，跨卷项明确拒绝并预告 v1.2——两个特性解耦，避免 v1.1 同时引入两套复杂度。
- **D5** 撤销依赖的日志格式在 FR-06 一次性定稿（JSONL + 事务号），避免二次迁移。
- **D6** 校验默认 size+mtime（快、零代价），SHA-256（BCrypt）作为配置项可选。
- **D7** 残局处理优先级：数据不丢失 > 不留残局 > 自动静默；回滚失败必须显式告知。
- **D8** 交互哲学延续：新增 UI 仅限“原生对话框补位”（进度、错误列表），不引入框架级 GUI。
